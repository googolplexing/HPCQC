# Copyright (c) 2026 Michael Mucciardi
# SPDX-License-Identifier: SSPL-1.0
#
# Q50 Benchmark: tfim_4q — noisy Aer simulation with Q50 noise model
# Uses Q50 calibration data to build a realistic noise model.
# density_matrix method required for noise simulation.
#
model: byo
model_params:
  hamiltonian_file: examples/byo_tfim_4q_q50.json

ansatz: su2
ansatz_params:
  reps: 1
  entanglement: linear

optimizer: spsa
optimizer_params:
  maxiter: 200

gradient: none

initializer: random
initializer_params:
  seed: 42

backend: aer_gpu
backend_params:
  method: density_matrix
  shots: 4096
  noise_model_file: examples/q50_calibration_20260326.json
precision: double
mode: interactive

checkpoint:
  enabled: true
  directory: checkpoints
  interval: 50

output_dir: results
